syntax = "proto3";
package pbgo;
import "github.com/AsynkronIT/protoactor-go/actor/protos.proto";

// 客服端
message ClientMessageReq {
    actor.PID Pid = 1; // 玩家actor的pid
    bytes Body = 2;
}

message ClientMessageRsp {
    actor.PID Pid = 1; // 
    bytes Body = 2;
}
